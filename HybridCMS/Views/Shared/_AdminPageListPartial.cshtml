@model IEnumerable<HybridCMSEntities.AdminPageListView>

@if (Model.Count() > 0)
{
    <small class="align-content-center">Pages</small>
    <br />
}

@foreach (var item in Model)
{
    <a href="" class="dropdown-item">
        @Html.DisplayFor(modelItem => item.Name)
    </a>
}
